services:
  kpi-api:
    build: ./kpi_api
    container_name: kpi-api
    ports:
      - "8000:8000"

  mcp-server:
    build: ./mcp_server
    container_name: mcp-server
    ports:
      - "8001:8001"